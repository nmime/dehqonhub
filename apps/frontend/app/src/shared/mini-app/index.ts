// Only the Telegram runtime context lives here. The generic product shell this
// folder used to export is gone: the site has a single DehqonHub layout, and a
// second header with its own navigation read as a different application.
export * from './mini-app-provider';
