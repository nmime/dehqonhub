# @app/backend-feature-agritech-shared

## Purpose

Owns AgriTech marketplace domain contracts, repository ports, and transition policies shared by application and persistence adapters.

## Commands

```bash
pnpm exec nx run @app/backend-feature-agritech-shared:test
pnpm exec nx run @app/backend-feature-agritech-shared:build
```
