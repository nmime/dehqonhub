# AgriTech PostgreSQL

## Purpose

Owns farmer, catalog, and order MikroORM adapters plus ordered reversible schema migrations.

Verify with `pnpm exec nx run @app/backend-postgres-main-agritech:test`.
